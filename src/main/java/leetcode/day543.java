package leetcode;
//求二叉树的最大直径
//二叉树的最大直径=所有节点为中心的直径的最大值
//MAXdiameter=MAth.max(length(Maxlength1~i))
//Maxlength(i)=Maxlength(i).left+Maxlength(i).right+2
public class day543 {

    public int diameterOfBinaryTree(TreeNode root) {
return  0;
    }
    public int maxdepth(ListNode node){
        return 0;
    }
}
