package leetcode;

public class day347前K个高频元素 {
    //先统计出每个数字的次数 O(n)，获取频率后，进行部分快排，二分选一半O(n)
    public int[] topKFrequent(int[] nums, int k) {
        new int
        for(int n:nums){

        }
    }

    //方法二：先统计出每个数字的次数 O(n)   然后堆排序
    public int[] topKFrequent(int[] nums, int k) {
        new int
        for(int n:nums){

        }
    }
}
